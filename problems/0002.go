package problems

/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */

 type ListNode struct {
	 Val int
	 Next *ListNode
 }

func AddTwoNumbers(l1 *ListNode, l2 *ListNode) *ListNode {
	// Check if 2 linked list have the same amount of elements
	// Get values and reverse values
	// Sum values
	// Reverse result
	// Put result in a linked list
	return &ListNode{2, nil}
}
